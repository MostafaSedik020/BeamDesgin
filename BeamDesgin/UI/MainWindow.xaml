<Window x:Class="BeamDesgin.UI.MainWindow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BeamDesgin.UI"
             mc:Ignorable="d" d:DesignWidth="800" Height="800"
             Loaded="Window_Loaded"
        Title="Beam Desgin"
        Background="Transparent"
        WindowStyle="None"
        AllowsTransparency="True"
        ResizeMode="NoResize"
        MouseLeftButtonDown="Window_MouseLeftButtonDown">

    

    <Border CornerRadius="25" BorderThickness="1" BorderBrush="Transparent" Background="Transparent">
        <Grid>
            <Grid.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                    <GradientStop Color="DarkGray" Offset="0.0"/>
                    <GradientStop Color="DarkGoldenrod" Offset="0.5"/>
                    <GradientStop Color="DarkGray" Offset="1.0"/>
                </LinearGradientBrush>
            </Grid.Background>
            <Grid.RowDefinitions>
                <RowDefinition Height="30"></RowDefinition>
                <RowDefinition Height="70"></RowDefinition>
                <RowDefinition Height="100"></RowDefinition>
                <RowDefinition Height="*"></RowDefinition>
                <RowDefinition Height="100"></RowDefinition>
            </Grid.RowDefinitions>

            <!-- Title bar with close button -->
            <Grid Grid.Row="0" Background="Transparent">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"></ColumnDefinition>
                    <ColumnDefinition Width="Auto"></ColumnDefinition>
                </Grid.ColumnDefinitions>
                <Label Content="Beam Desgin" VerticalAlignment="Center" Margin="10,0,0,0" FontSize="16" FontWeight="Bold" FontFamily="Arial" Foreground="Black" Grid.Column="0"/>
                <Button Content="X" Width="30" Height="30" Click="CloseButton_Click" Background="Transparent" BorderBrush="Transparent" Foreground="Black" Grid.Column="1"/>
            </Grid>

            <Button Name="Browse_btn" Content="Browse" HorizontalAlignment="Left" Margin="469,29,0,0" Grid.Row="1" VerticalAlignment="Top" Width="70" Height="30" Click="Browse_Button_Click"/>
            <TextBox x:Name="Path_TxtBox" HorizontalAlignment="Left" Height="20" Margin="39,34,0,0" Grid.Row="1" TextWrapping="Wrap"  VerticalAlignment="Top" Width="384"/>

            <!--check boxes-->
            <Expander x:Name="RebarExpander" HorizontalAlignment="Left" Height="100" Header="Choose Rebar" Margin="24,10,0,0" Grid.RowSpan="2" Grid.Row="2" VerticalAlignment="Top" Width="400" >
                <Grid Background="Gray">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"></RowDefinition>
                        <RowDefinition Height="*"></RowDefinition>
                        <RowDefinition Height="*"></RowDefinition>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"></ColumnDefinition>
                        <ColumnDefinition Width="*"></ColumnDefinition>
                        <ColumnDefinition Width="*"></ColumnDefinition>
                    </Grid.ColumnDefinitions>
                    <CheckBox Content="8" Grid.Row="0" Grid.Column="0" />
                    <CheckBox Content="10" Grid.Row="0" Grid.Column="1"/>
                    <CheckBox Content="12" Grid.Row="0" Grid.Column="2"/>
                    <CheckBox Content="14" Grid.Row="1" Grid.Column="0"/>
                    <CheckBox Content="16" Grid.Row="1" Grid.Column="1"/>
                    <CheckBox Content="18" Grid.Row="1" Grid.Column="2"/>
                    <CheckBox Content="20" Grid.Row="2" Grid.Column="0"/>
                    <CheckBox Content="22" Grid.Row="2" Grid.Column="1"/>
                    <CheckBox Content="25" Grid.Row="2" Grid.Column="2"/>
                    
                </Grid>
            </Expander>

            <!-- Data after sorting -->
            <DataGrid x:Name="BeamDataGrid" Grid.Row="3" AutoGenerateColumns="False" IsReadOnly="True" Margin="10">
                <DataGrid.Resources>
                    <Style TargetType="{x:Type DataGridColumnHeader}">
                        <Setter Property="Background" Value="PaleVioletRed"/>
                        <Setter Property="FontWeight" Value="SemiBold"/>
                        <Setter Property="BorderThickness" Value="0,0,1,2"/>
                        <Setter Property="BorderBrush" Value="Black"/>
                    </Style>
                </DataGrid.Resources>

                

                <DataGrid.Columns>

                    <DataGridTextColumn Header="BeamMark"  Binding="{Binding BeamMark}" Width="*" />
                    <DataGridTextColumn Header="size Breadth" Binding="{Binding Breadth}" Width="*"/>
                    <DataGridTextColumn Header="sizeDepth" Binding="{Binding Depth}" Width="*"/>
                    <DataGridTextColumn Header="Bot left" Binding="{Binding BotAsCornerData}" Width="*"/>
                    <DataGridTextColumn Header="Bot mid" Binding="{Binding BotAsMidData}" Width="*"/>
                    <DataGridTextColumn Header="Bot right" Binding="{Binding BotAsCornerData}" Width="*"/>
                    <DataGridTextColumn Header="Top left" Binding="{Binding TopAsCornerData}" Width="*"/>
                    <DataGridTextColumn Header="Top mid" Binding="{Binding TopAsMidData}" Width="*"/>
                    <DataGridTextColumn Header="Top right" Binding="{Binding TopAsCornerData}" Width="*"/>
                    <DataGridTextColumn Header="Links Left" Binding="{Binding Corner_Astr_Data}" Width="*"/>
                    <DataGridTextColumn Header="Links Mid" Binding="{Binding Mid_Astr_Data}" Width="*"/>
                    <DataGridTextColumn Header="Links Right" Binding="{Binding Corner_Astr_Data}" Width="*"/>

                </DataGrid.Columns>
            </DataGrid>

            <Button x:Name="desgin_btn" Content="Desgin" HorizontalAlignment="Left" Margin="469,40,0,0" Grid.Row="2" VerticalAlignment="Top" Width="70" Height="30" Click="desin_btn_click"/>
            <CheckBox x:Name="ItemizeCheckBox"  Content="Itemize Every Object" HorizontalAlignment="Left" Margin="24,10,0,0" Grid.Row="4" VerticalAlignment="Top" Checked="ItemizeCheckBox_Checked" Unchecked="ItemizeCheckBox_Unchecked" Visibility="Collapsed"/>

        </Grid>
    </Border>
</Window>
